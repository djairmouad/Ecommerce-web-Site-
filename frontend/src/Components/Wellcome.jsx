export default function Welcome({name}) {
    return (
        <div id="wellcome" >
            <h3 className="font-medium">Hello, <span className="text-4xl font-semibold">{name}</span></h3>
        </div>
    );
}
